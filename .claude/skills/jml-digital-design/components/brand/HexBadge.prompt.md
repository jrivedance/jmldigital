Stat sticker in the brand hexagon, gradient blue on white.

```jsx
<HexBadge value="403%" label="more inquiries" />
```

This is the one place a saturated blue fill is allowed at size. One per photo, pinned to a corner with negative margins.
