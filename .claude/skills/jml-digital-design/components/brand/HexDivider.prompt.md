The honeycomb section break from the marketing site.

```jsx
<HexDivider style={{ paddingTop: 32 }} />
```

Four density variants ship in `assets/images/`: `JMLHex1–3.png` (denser clusters) and `JMLHex4.png` (the wide, sparse strip used under the hero). Always 80% opacity, centred, max 768px. Use at most twice per page.
