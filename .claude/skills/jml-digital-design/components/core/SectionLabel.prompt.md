Sets up a section. Always the first element, followed by an `<h2>` 24px below.

```jsx
<SectionLabel>What We Offer</SectionLabel>
```

Title Case copy, 12px, `0.2em` tracking, 50% black on a glass pill. One per section — never stack two.
