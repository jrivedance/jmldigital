Admin dashboard metric tile.

```jsx
<StatCard label="Upcoming Shoots" value="7" icon={<Camera size={18} />} />
```

Labels are uppercase and terse; values are pre-formatted strings (currency with `$` and thousands separators). Four across on desktop, two on tablet.
